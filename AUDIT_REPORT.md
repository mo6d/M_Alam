# تقرير التدقيق الشامل لتطبيق Al-Masrya

**تاريخ التدقيق:** 26 أغسطس 2026

**النطاق:** تطبيق Flask/SQLite العربي لإدارة المخزون والمبيعات والتوريد والعملاء والموردين والخزانة والحسابات والتقارير والصلاحيات والنسخ الاحتياطية.

## الخلاصة التنفيذية

أُنجز تدقيق وظيفي وحسابي ونصي وبصري على نسخة اختبارية معزولة. الحالة النهائية للنسخة المعدلة جيدة وفق الاختبارات المنفذة: نجح فحص بناء التطبيق، واختبار الصفحات الأساسية، واختبار الربح والخسارة، و58 فحصاً آلياً شاملاً، و27 عملية تعديل، واختبار اتساق مرتجع البيع، واختبار حواجز الصلاحيات، وكلها دون إخفاقات.

لم تُستخدم قاعدة بيانات المستخدم الأصلية في الاختبارات. أنشئت قواعد مؤقتة عبر `ALMASRYA_BASE_DIR` داخل `/tmp`، كما شُغّلت الواجهة البصرية على قاعدة اختبار منفصلة. لذلك لا تثبت النتائج سلامة كل جهاز فعلي أو كل بيئة تشغيل ممكنة، لكنها تغطي منطق التطبيق ومساراته الرئيسية بدرجة واسعة وقابلة لإعادة التشغيل.

## نطاق ما تم فحصه

| المجال | ما تم فحصه |
|---|---|
| الصفحات والروابط | مسارات لوحة التحكم، المنتجات، التصنيفات، المخازن، الموردين، العملاء، المبيعات، التوريد، الديون، الحسابات، الخزانة، التقارير، الربح والخسارة، التحليل التفصيلي، الجرد، حركة المخزون، الإعدادات، المستخدمين، سجل العمليات، النسخ الاحتياطية، وصفحات الفواتير والإيصالات. |
| العمليات | إضافة وتعديل المنتجات والجهات والفواتير، الدفعات، المرتجعات، إلغاء الفواتير، التحويل، التسوية، الجرد، الخزانة، المستخدمون والصلاحيات، الإعدادات، النسخ الاحتياطية. |
| الحسابات | التكلفة المثبتة في بند البيع، المتوسط المرجح عند التوريد، خصم مرتجعات البيع والشراء، صافي الربح والخسارة، المصروفات والدخل الإضافي، الذمم والسيولة وقفل الفترات. |
| الأمان والصلاحيات | CSRF، وضع المشاهدة، صلاحيات المستخدمين، حماية مسارات الفواتير والمخزون وجهات الاتصال، وإتاحة صفحات العرض دون فتح عمليات التعديل. |
| العرض والنصوص | 13 شاشة رئيسية على واجهة محلية، منها الدخول، تغيير كلمة المرور، لوحة التحكم، المنتجات، فاتورة البيع، التقارير، التسوية، المستخدمون، النسخ الاحتياطية، الحسابات، الربح والخسارة، الخزانة، والتحليل التفصيلي. |
| التصدير والاعتماديات | CSV وXLSX، مع إضافة الاعتماد المفقود `openpyxl==3.1.5` إلى `requirements.txt`. |

## الاختبارات والنتائج

| الاختبار | النتيجة |
|---|---:|
| `python3 -m py_compile النسخة ما قبل الاخيرة/app.py` | نجح |
| `test_all_pages.py` | نجح |
| `test_profit_loss.py` | نجح |
| `full_audit.py` | 58 فحصاً ناجحاً، 0 إخفاق |
| `full_action_audit.py` | 27 عملية ناجحة، 0 إخفاق |
| `test_detailed_return_consistency.py` | نجح؛ مرتجع جزئي ظهر مرة واحدة وبالقيم الصحيحة |
| `test_permissions.py` | نجح؛ الموظف محدود الصلاحيات مُنع من العمليات غير المصرح بها |
| فحص عناوين القوالب الثابت | لا توجد استدعاءات `render_template` بلا عنوان صفحة |

## الأخطاء المؤكدة التي تم إصلاحها

### عناوين الحسابات والربح والخسارة والخزانة

كانت بعض المسارات لا تمرر المتغير `title` إلى القالب، فكان عنوان المتصفح والرأس يظهران بصيغة عامة مثل «لوحة التحكم» أو `Al-Masrya | Al-Masrya`. تمت إضافة عناوين واضحة إلى الحسابات، وتقرير الربح والخسارة، والتحليل التفصيلي، والخزانة، وتفاصيل دخل الخزانة، وتفاصيل مصروفاتها. تم التحقق بصرياً من العناوين المصححة بعد إعادة تشغيل التطبيق.

### تناقض وصف تكلفة البيع

كان النص المحاسبي في تقرير الربح والخسارة يقول إن الحساب يعتمد على «سعر التكلفة الحالي»، بينما منطق التطبيق يحفظ تكلفة بند البيع وقت إنشاء الفاتورة مع استخدام التكلفة الحالية كمرجع احتياطي للبيانات القديمة. تم تصحيح النص ليصف المنطق الفعلي بوضوح.

### عدم اتساق التحليل التفصيلي مع المرتجعات

كان التحليل التفصيلي يعرض بيانات البيع الأصلية دون خصم المرتجعات، ولذلك كان يمكن أن يعرض مبيعات وربحاً أعلى من الصافي الفعلي. تمت مواءمته مع تقرير الربح والخسارة بحيث يخصم مرتجعات البيع والشراء، ويستخدم إجمالي الفاتورة بعد الخصم والضريبة، ويطرح تكلفة الكمية المرتجعة من التكلفة المثبتة. كما تم فصل تجميع مبلغ المرتجع عن تجميع بنوده لمنع تكرار قيمة المرتجع عندما يحتوي على أكثر من صنف.

### فجوات في الصلاحيات

كانت بعض عمليات الإضافة والتعديل تعتمد على تسجيل الدخول فقط أو على فحص دور عام، رغم وجود نظام صلاحيات تفصيلي. تم إصلاح ذلك كما يلي:

| العملية | الحماية النهائية |
|---|---|
| إضافة وتعديل المنتجات والجرد والتحويل والتسوية والتصنيفات والمخازن | صلاحية إدارة المخزون |
| إضافة العملاء والموردين | صلاحية إدارة جهات الاتصال |
| إنشاء فواتير البيع والتوريد | صلاحية تعديل الفواتير |
| حذف التصنيف | المدير فقط، مثل حذف بقية الكيانات الحساسة |
| عرض الصفحات | بقي متاحاً للمستخدمين الذين يملكون صلاحية العرض، دون فتح عمليات POST غير المصرح بها |

### اعتماد XLSX المفقود

كان `openpyxl` مستخدماً فعلياً لتصدير تقرير الربحية إلى XLSX لكنه غير موجود في `requirements.txt`. تمت إضافته بإصدار `3.1.5`، وهو الإصدار الذي تم التحقق من توفره وتشغيل التصدير به في بيئة التدقيق.

### صياغة غير واضحة في نصائح الخزانة

تم استبدال عبارات عامية أو غير دقيقة مثل «معاك الآن» و«اختر تاريخ مين وإلى» بصياغة عربية أوضح: «الرصيد الحالي»، و«اختر تاريخ البداية والنهاية»، مع توضيح التسجيل اليومي للحركات.

## ملاحظات استخدام مهمة وليست أخطاء مؤكدة

تقرير الحسابات يعرض قيمة المخزون بسعر التكلفة الحالي، وهذا صحيح لأنه يقيس قيمة المخزون الموجود حالياً، بينما تقرير الربح والخسارة يعتمد على تكلفة البيع المثبتة تاريخياً. لا ينبغي تفسير «صافي المركز التشغيلي» على أنه قائمة مالية رسمية؛ الصفحة نفسها توضّح أنه مؤشر إداري.

يعرض التطبيق بيانات الدخول الأولية للمدير في صفحة الدخول، ويفرض تغيير كلمة المرور بعد أول دخول. ينبغي تغيير كلمة المرور فوراً في التشغيل الفعلي وعدم مشاركة بيانات الدخول الأولية.

يجب أن يكون تاريخ الحركة واضحاً عند استخدام قفل الفترة المالية؛ التطبيق يمنع تسجيل الحركات الواقعة داخل الفترة المغلقة وفق الإعداد المسجل. أما استعادة نسخة احتياطية فعلية لبيانات الإنتاج فلم تُنفذ على قاعدة المستخدم حفاظاً على بياناته، واختبار الاستعادة الذي تم سابقاً كان على ملفات اختبار معزولة.

## حدود الضمان

لا يمكن من هذه البيئة ضمان السلوك على كل هاتف أو قارئ باركود أو طابعة أو إعداد متصفح أو نسخة نظام تشغيل. تمت معاينة الواجهة المحلية في نافذة متصفح واحدة، مع فحص قواعد CSS والمسارات الرئيسية، لكن الاختبار اليدوي الشامل لكل طراز هاتف وكل جهاز طرفي يحتاج إلى أجهزة فعلية. كما لم يتم نشر التطبيق على خادم خارجي أو اختباره تحت حمل متزامن حقيقي.

## الملفات المعدلة أو المضافة

| الملف | التغيير |
|---|---|
| `app.py` | عناوين الصفحات، اتساق التحليل التفصيلي مع المرتجعات، وحماية الصلاحيات. |
| `templates/profit_loss.html` | تصحيح شرح التكلفة المثبتة تاريخياً. |
| `templates/treasury.html` | تحسين العبارات التوضيحية ونصائح الاستخدام. |
| `requirements.txt` | إضافة `openpyxl==3.1.5`. |
| `AUDIT_REPORT.md` | هذا التقرير. |

## محتوى الحزمة النهائية

الحزمة النهائية مبنية من مجلد المشروع المعدل، وتستبعد `__pycache__` وملفات التدقيق المؤقتة. لم تُنشأ داخل مجلد المشروع أي قاعدة اختبار؛ قواعد الاختبار المستخدمة كانت في `/tmp`. تم الإبقاء على ملفات المشروع الأصلية المطلوبة لتشغيل النسخة كما هي، بما فيها قاعدة بيانات المستخدم وملفات النسخ الاحتياطية الموجودة قبل التدقيق، دون تعديل محتواها أثناء الاختبارات.

> **الخلاصة:** لم تظهر أخطاء فشل في الاختبارات النهائية، وتم إصلاح الأخطاء المؤكدة التي ظهرت أثناء المراجعة. ومع ذلك، يجب أخذ حدود الاختبار المذكورة أعلاه في الاعتبار قبل اعتبار التطبيق معتمداً إنتاجياً دون تجربة تشغيلية على جهاز المستخدم ونسخة احتياطية مستقلة.


## إضافة تدقيق بتاريخ 2 سبتمبر 2026

أُعيد تشغيل النسخة بعد استلامها على بيئة اختبار نظيفة. تم اكتشاف وإصلاح خطأ إقلاع كان يظهر عندما يحدد المستخدم مجلداً جديداً عبر المتغير `ALMASRYA_BASE_DIR`: كانت SQLite تحاول فتح `almasrya.db` قبل إنشاء المجلد الأب. أصبحت النسخة تنشئ مجلد البيانات تلقائياً قبل أول اتصال بقاعدة البيانات، وهو ما يدعم التشغيل المحمول والاختبارات والتثبيت في مسار جديد.

تم بعد الإصلاح تشغيل `python3 -m py_compile app.py` بنجاح، وإقلاع الخادم فعلياً على مجلد جديد، وفحص 70 مساراً و38 قالباً، مع نجاح الصفحات الأساسية، واجهات API غير الموجودة بإرجاع 404 صحيح، وإنشاء التصنيف والمخزن والمورد والعميل والمنتج، ثم إنشاء فاتورة توريد وفاتورة بيع والتحقق من تحديث رصيد المخزون إلى القيمة المتوقعة. كما تم التحقق من وجود حقل CSRF في نماذج POST وعدم وجود استدعاءات `url_for` تشير إلى مسارات غير معرفة.

### الملفات المعدلة في هذه الجولة

| الملف | التغيير |
|---|---|
| `app.py` | إنشاء `BASE_DIR` تلقائياً قبل تهيئة SQLite لمنع فشل الإقلاع في المسارات الجديدة. |
| `AUDIT_REPORT.md` | توثيق الفحص الأخير والإصلاح ونتائج الاختبارات. |

> لم تُستخدم قاعدة بيانات المستخدم أو ملفاته التشغيلية في الاختبارات؛ أُنشئت قواعد مؤقتة معزولة داخل `/tmp`.


## نتائج الاختبار الإضافي بتاريخ 2 سبتمبر 2026

تم تنفيذ اختبار تكاملي معزول شمل العمليات التي لم تكن ضمن اختبار الدفعة الأولى: إنشاء دفعات البيع والتوريد، المرتجع الجزئي للبيع، تعديل فاتورة تحتوي على مرتجع، إلغاء الفاتورة، الجرد، التحويل بين مخزنين، التسوية اليدوية، إضافة وحذف حركة خزانة، واستدعاء تقارير CSV وXLSX وتنزيل النسخة الاحتياطية. نجحت جميع العمليات مع تحقق قواعد الاتساق الأساسية في قاعدة البيانات.

تم أيضاً اختبار استعادة نسخة SQLite صالحة واستبعاد ملف غير صالح، واختبار إدخال قيم غير رقمية في التسوية دون خطأ خادم. واختُبرت صلاحيات Viewer والموظف ذي الصلاحية المحدودة؛ مُنعت عمليات التعديل غير المصرح بها مع استمرار الوصول إلى التقرير المسموح به. كما تم تنفيذ الدخول وتغيير كلمة المرور إجبارياً في المتصفح ثم فحص لوحة التحكم بصرياً، وظهرت الواجهة والقائمة الجانبية والبطاقات البيانية بصورة سليمة في نافذة الاختبار.

> تظل الاختبارات على الأجهزة الفعلية، والطابعات، وقارئات الباركود، وأحمال التزامن العالية خارج نطاق البيئة المعزولة.
